<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <ul>
    <% for (var i = 0; i < messages.length ; i++) { %>
        <li> <%= messages[i].text %><br> by user <b><%= messages[i].user %></b> on <%= messages[i].added %> </li>
    <% } %>
  </ul>
  <a href="/new">Click to add message</a>
  </body>
</html>
